name: Testing
on: 
  push

jobs:
    jobConditional:
        runs-on: ubuntu-latest
        if: ${{ secrets.SECRET_EXISTS != ''}}
        steps:
            - run: echo "This ran"
    stepConditional:
        runs-on: ubuntu-latest
        steps:
            - run: echo "This ran"
              if: ${{ secrets.SECRET_EXISTS != ''}}